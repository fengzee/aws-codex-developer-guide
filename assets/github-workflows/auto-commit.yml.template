name: Generated content auto commit

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up project
        run: |
          # TODO: install dependencies for the generator.
          # Examples:
          # npm ci
          # pip install -r requirements.txt
          true

      - name: Run deterministic generator
        run: |
          # TODO: replace this with a deterministic command.
          # Examples:
          # npm run generate
          # python scripts/update_report.py
          true

      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: update generated content"
          git push
